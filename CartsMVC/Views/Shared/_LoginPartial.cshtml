
@if (Request.IsAuthenticated)
{
<ul class="nav navbar-nav navbar-right">
    <li>
        @Html.ActionLink("Hello " + User.Identity.Name + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
    </li>
    <li>
        @Html.ActionLink("我的訂單", "MyOrder", "Order", routeValues: null, htmlAttributes: new { title = "Order" })
    </li>
    <li>@Html.ActionLink("登出", "Logout", "Account")</li>
</ul>
}
else
{
<ul class="nav navbar-nav navbar-right">
    <li>@Html.ActionLink("註冊", "Register", "Account", null, new { id = "registerLink" })</li>
    <li>@Html.ActionLink("登入", "Login", "Account", null, new { id = "loginLink" })</li>
</ul>
}

